;(function (exports) {

    // App base config
    var Config = {
        // playground width
        WIDTH: 660,

        // playground height
        HEIGHT: 400,

        // per grub piex
        UNIT: 20,

        // per apple could achive scores
        SCORE: 2,

        // init snake length, except head
        LENGTH: 1,

        // move speed
        SPEED: 178
    }

    exports.AppConfig = Config
})(window)
