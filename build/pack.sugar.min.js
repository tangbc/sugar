import { minify } from 'uglify-js';
import buble from 'rollup-plugin-buble';
import uglify from 'rollup-plugin-uglify';
import { sugarBanner, outputConfig } from './pack.banner';

export default {
	entry: './src/main/index.js',
	dest: './dist/sugar.min.js',
	format: 'umd',
	moduleName: 'Sugar',
	banner: sugarBanner,
	plugins: [
		buble(),
		uglify({
			output: outputConfig
		}, minify)
	]
}
